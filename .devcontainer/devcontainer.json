{
  "name": "Ken_CirWeb",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "sudo chown -R node /workspace && pnpm config set store-dir /home/node/.local/share/pnpm/store && pnpm install --frozen-lockfile",
  "customizations": {
    "vscode": {
      "extensions": [
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint",
        "EditorConfig.EditorConfig",
        "Vue.volar"
      ]
    }
  }
}
